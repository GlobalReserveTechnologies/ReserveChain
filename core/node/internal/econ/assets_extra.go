package econ

// Additional crypto asset constants for mainnet-style reserve baskets.
// These are declared in a separate file so they can evolve without
// modifying the original reserve_pools_crypto.go declarations.

const (
    AssetSTETH CryptoAssetKind = "STETH"
)
